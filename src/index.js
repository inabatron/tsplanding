import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // Add any custom styles if needed
import App from './App';


ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);